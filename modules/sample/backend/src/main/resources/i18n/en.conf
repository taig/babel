include "*"

app = {
  name = "Babel"
}

index = {
  page = {
    title = "Sample"
    description = "Check out this sample app"
  }
  headline = "Hello World"
  introduction = "The content of this page will be displayed in either English or German, depending on the preference in the \"Accept-Language\" header of your request. The click counter, on the other hand, is translated dynamically using JavaScript. To do this, the website first requests the appropriate language file from the server using AJAX. The counter also shows an example of the use of the plural and string format functions."
  message = {
    1 = "$1 click"
    "*" = "$1 clicks"
  }
  label = "Click me!"
}